{
    "rules": {
      	".read": "true",
        "tournaments": {
          	"$tid": {
              ".write": "!data.exists() || data.child('creator_uid').val() === auth.uid"
            }
        }
    }
}